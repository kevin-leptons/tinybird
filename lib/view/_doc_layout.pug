extends _layout.pug

block body-content
    div(class='search-box')
        form(action='/doc')
            input(id='search', type='text', name='keyword', placeholder='search')

    block doc-result
